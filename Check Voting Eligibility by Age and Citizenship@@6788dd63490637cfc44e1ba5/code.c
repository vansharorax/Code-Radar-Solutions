#include <stdio.h>
int main() {
    int age;
    int citi;
    scanf("%d",&age);
    if (age >= 18 && citi ==1) {
        printf("Eligible");
    } else {
        printf("Not Eligible");
    }
}