#include <stdio.h>
int main() {
    int a;
    int b;
    scanf("%d %d",&a ,&b);
    if ( a== 10 && b == 5) {
        printf("Yes");
    } else {
        printf("No");
    }
    return 0;
}