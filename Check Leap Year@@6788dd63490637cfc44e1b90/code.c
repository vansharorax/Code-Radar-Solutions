#include <stdio.h>
int main() {
    int year;
    scanf("%d",&year);

    if ( year = 2000 ; 1900 ; 2023 ) {
        printf("Leap Year");
    } else {
        printf("Not a Leap Year");
    }
    return 0;
}